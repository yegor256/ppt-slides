name: latexmk
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: yegor256/latexmk-action@master
        with:
          opts: -pdf ppt.tex
          packages: libertine microtype fancyhdr href-ul minted fvextra framed tcolorbox environ catchfile xstring pgfopts crumbs tikz qrcode varwidth
